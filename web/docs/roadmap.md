# 路线图

当前版本聚焦「简单、健壮、够用」。以下按优先级列出计划中的能力。

## 近期

- [ ] **中继 ↔ 直连无缝切换**：运行在中继（TURN/私有 TCP）上时周期重评估，直连恢复即自动升级（数据面统一 UDP，切换成本低）
- [ ] **TURN 供应商自动发现**：信令服务器下发可用 TURN 列表，客户端自动选择
- [ ] **并发多连接复用**：同一隧道同时承载多条本地 TCP 连接（当前为顺序复用）
- [ ] **WebSocket 实时信令**：房主即时感知访客加入，替代轮询（降低直连建立延迟）
- [ ] **UPnP 自动端口映射**：打洞前的补充手段，路由器支持时直接映射端口

## 中期

- [ ] **x25519 密钥交换**：免口令自动协商会话密钥（替代手工 `--key`）
- [ ] **中继通道加密**：私有 TCP 中继路径的端到端机密性（当前 TCP 中继仅有密码级流加密，`--key` 作用于 UDP 数据面：直连与 TURN）
- [ ] **传输参数自适应**：根据丢包/时延自动调整窗口、重传间隔、MTU

## 远期

- [ ] **平台化**：Windows/macOS 安装包、开机自启、托盘图标
- [ ] **中继集群**：多服务器中继池 + 就近选路
- [ ] **兼容层**：接入现有协议生态（可选）

## 已实现的能力（v0.2）

-  TURN 中继（v0.2 主线）：
  - 内置 TURN 服务端（`serve --turn`，RFC 5766 UDP 子集，认证复用 `--password`）
  - 客户端**多 TURN 供应商**（`turn_providers`：内置 / 自建 coturn / Cloudflare TURN 等）
  - **自动选优**：建立时并行 Allocate 测 RTT，选最快可用供应商
  - **TURN 回退链**：打洞失败（含 `--relay`）→ TURN 中继（信令交换 relay 地址）→ 私有 TCP 兜底
  - STUN 公网地址学习（`stun_addr`，免费、不依赖自建 UDP 探测）
  - 待办：运行中自动升降级（中继 ↔ 直连无缝切换）、TURN 供应商自动发现

## 已实现的能力（v0.3）

-  **连接配置档案**（`frp-sh profile add/list/show/edit/remove/run`）：一行保存服务器+密码+房间，名字顺序分配可改名；同服务器+模式自动去重合并；面板"一键接入"底层即 profile
-  **双端 Web 面板**：服务器面板（在线房间/设备/链路速率/拓扑图、一键接入命令、日志视图）+ 客户端面板（本机状态/分享/日志视图）；密码全程打码，复制时才落剪贴板
-  **日志系统**：终端静默，日志写入 `<配置目录>/logs/frp-sh.log`（5MB 轮转）+ 内存环形缓冲，面板 `/debug` 增量拉取；服务器侧关键事件（join/relay 配对/超时/房间移除）全程留痕
-  **打洞降级策略**（`--punch-retries`，默认 1）：打洞失败一次即跳过打洞与 TURN，直接走 TCP 中继，不再重复失败路径；直连掉线自动跳过打洞
-  中继 WAIT 死锁修复（UUID 槽位等待者防误唤醒）、中继流量统计口径修复、同 NAT 场景链路稳定性排查工具（面板 debug）
-  一键安装脚本（Windows PowerShell / POSIX shell），安装即用（跳过向导，后续用 profile/面板配置）

## 已实现的能力（v0.1）

-  房间制信令（REST + UDP 探测 + TTL 过期）
-  UDP 打洞（PUNCH/ACK 同时握手 + 端口散布）
-  FRS1 可靠流（滑动窗口 + 重传 + keepalive + FIN 握手）
-  ChaCha20-Poly1305 端到端加密（`--key`）
-  顺序多连接复用（`--max-conns`）
-  TCP 中继回退（配对 + 双向拷贝 + 迟到直连复查）
-  断线自动重连（心跳存活检测：流级 3s 无帧判定死亡，`FRPSH_LIVENESS_MS` 可调；指数退避重连 + 刷新公网地址 + 打洞窗口随地址变化重开；中继 TCP 短周期保活）
-  多访客组网（`lan` 一个房间 1 host + N guests 全互联：host 为枢纽按目标 IP 转发，每访客直连优先/中继兜底，UUID 键控中继配对）
-  同局域网自动直连（通告局域网地址，双路打洞）
-  组网模式（`lan` 系列：虚拟网卡整机入网，10.66.0.0/24；访客可访问房主局域网）
-  设备唯一 ID（UUID）+ 稳定派生虚拟 IP + 房主 IP 池（`--guest-ips`）
-  服务端密码认证（`serve --password`：请求 401 校验 + 中继认证）
-  中继流量加密（ChaCha20-Poly1305 流加密，防嗅探）
-  版本治理：协议版本冲突控制（`/version`）、启动更新检查（可跳过/代差强制）、服务器/房主版本黄红提示
-  局域网接入默认不暴露（`--expose-lan` 显式开启）
-  Windows 兼容（ICMP 毒化处理、TUN/WinTun、自动 UAC 提权、防火墙自动放行）
-  macOS utun 兼容（点对点路由修复）
-  CLI 全英文 + 彩色输出 + ASCII 标识 + 交互式安装初始化
-  端到端测试与真实 NAT 实测

## 贡献

欢迎提交 PR。开发指引见[开发与测试](./develop.md)，协议细节见[协议规范](./protocol.md)。
