# v0.5.4

- Add server-enforced room, per-room member and total member admission limits. Counts include owners; reconnects reuse registered slots. Defaults preserve previous capacity.
- Return explicit capacity errors and advertise configured limits in `/version`.
- Align Chinese and English documentation with current room workflows, protocol v3, relay behavior and explicit updates.
- Signaling protocol v3 and helper protocol v2 remain unchanged.
