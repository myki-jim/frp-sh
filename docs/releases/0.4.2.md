# frp-sh 0.4.2

修复 macOS ping 自身虚拟 IP 时经房主和中继绕行的问题。

- 网络辅助服务为自身虚拟 IPv4 地址添加精确的回环路由，其他成员地址仍经 utun。
- 路由随设备会话创建与清理；添加失败会中止设备初始化，不覆盖已有路由。
- macOS CI 新增真实辅助服务验收：无对端自 ping 成功、自身路由为 lo0、对端路由为 utun、utun 无自 ping 数据、会话结束无回环路由残留。

Mac 用户须重新运行安装器升级网络辅助服务，仅替换主程序不能修复这个问题。协议仍为 v2，兼容 0.4.0 服务端与 0.4.1 客户端。
